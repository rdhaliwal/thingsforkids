$('#listings').prepend("<%= j render partial: 'home/listing', collection: @listings %>");

<% if @listings.next_page %>
  $('.pagination').replaceWith('<%= j paginate @listings %>');
<% else %>
  $('.pagination').remove();
<% end %>
